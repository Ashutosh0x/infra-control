server:
  http_port: 8080
  grpc_port: 9090

database:
  postgres:
    connection: "postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:5432/${DB_NAME}?sslmode=require"

logging:
  level: "info"
  format: "json"

mcp:
  auth:
    enabled: true
